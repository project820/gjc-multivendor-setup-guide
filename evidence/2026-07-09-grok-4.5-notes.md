# Grok 4.5 landing — evidence notes (2026-07-09, gjc 0.9.1)

Append-only. Backs the v1.10.0 Tier-1 critic refresh (grok-4.3 → grok-4.5). Corrections to
this file are made by a later dated errata, never by rewriting it.

## 1. Catalog & selector (GJC `gjc --list-models grok`, gjc 0.9.1)
- Canonical `grok-4-5` → selected `xai/grok-4.5`. **xai API provider only** — there is
  **no** `grok-build/grok-4.5` (SuperGrok-OAuth) variant. Requires `XAI_API_KEY` / xai /login.
- GJC catalog reports: context **222K**, max-out **8.9K**, thinking column `-`.
- Catalog diff `evidence/2026-07-03-catalog.txt` → `evidence/2026-07-09-catalog.txt`: the ONLY
  change is grok-4.5 added. GJC 0.7.10→0.9.1 otherwise left the model surface unchanged.

## 2. Provider source of truth (Grok CLI `~/.grok/models_cache.json`, model `grok-4.5`)
GJC catalog metadata for grok-4.5 is STALE (same lag pattern as the wrong `thinking: -`).
Provider cache says:
- `context_window: 500000`  → **provider context is 500K, not 222K.**
- `auto_compact_threshold_percent: 80` → auto-compacts at ~400K. **Exact-diff-safe critic
  ceiling ≈ 400K** (compaction squashes line references above it; route larger exact-diff
  review to 1M lanes: Opus / Gemini / GPT-5.4 / GLM).
- `max_completion_tokens: null` → no fixed output cap exposed (GJC's 8.9K is its own stale default).
- `supports_reasoning_effort: true`; `reasoning_efforts` = **low / medium / high (default high)**.
  There is **no native xhigh tier**.
- Operating stance: record both; treat GJC's 222K as a conservative floor until a large-context
  real `gjc -p` call confirms the effective GJC window.

## 3. Effort behavior (real `gjc -p --no-session --no-tools`)
| selector | exit | note |
| --- | --- | --- |
| xai/grok-4.5:medium | 0 (OK) | shipped (daily.critic) |
| xai/grok-4.5:high   | 0 (OK) | shipped (ultimate/ultimate-f5/legend/escalation.critic + cyber-cop panel) |
| xai/grok-4.5:low    | 0 (OK) | native, not shipped |
| xai/grok-4.5:xhigh  | 0 (OK) | **accepted but clamps to high** (no native xhigh) — NOT shipped |
| xai/grok-4.5:max    | 0 (OK) | **alias → clamps to high** — NOT shipped |
| xai/grok-4.5:bogus  | 1 (not found) | effort tokens ARE validated |
Confirmed 2026-07-09. A live session with `default_reasoning_effort="xhigh"` behaved at
~medium magnitude; the TUI menu exposes only low/medium/high. => grok-4.5 effective ceiling = high.
`validate-profiles.py` therefore forbids grok-4.5 `xhigh`/`max` as profile efforts.

Selector evidence files:
- `2026-07-09-selectors.md` is the first same-day run. It falsely labeled Anthropic weekly
  rate-limit 429 responses as regressions because the script did not yet classify rate limits
  as `blocked`.
- `2026-07-09-selectors-rerun-2.md` is the authoritative rerun after that script fix:
  grok-4.5 `:medium`/`:high` OK; `:bogus` fails; OpenAI/Gemini/opencode rows OK; Anthropic
  rows are `blocked(creds/rate-limit)`, not model regressions.

## 4. Latency / cost bench (2026-07-09, cli-chat-proxy, short EN generation prompt)
| effort | reasoning tokens | TTFT | total | note |
| --- | --- | --- | --- | --- |
| low     | 585   | 6.2s  | 10.0s | |
| default (~medium) | ~1.7k | 12.6s | ~14s | |
| high    | 7,608 | 48.3s | 49.7s | max-depth; the cost premium profiles buy |
Decode ~150 tok/s (reasoning) / ~210 tok/s (body). Perceived slowness = reasoning LENGTH,
not decode speed. => daily.critic uses `:medium` (responsive); cost-ignore/high-stakes
profiles use `:high`.

## 5. Price (OpenRouter x-ai/grok-4.5, xAI provider)
- List: **$2.00 in / $6.00 out** per 1M, cache $0.50.
- 30-day effective after 88.1% cache hit: ~**$0.84 in / $6.20 out**.
- vs incumbent grok-4.3 critic ($1.25 in / $2.5 out): grok-4.5 output ~2.4× pricier list;
  input cheaper effectively with caching. Acceptable for a cross-family critic.
- Uptime ~98.85% (3d), throughput 84–108 tok/s, TTFT p50 0.62–0.91s (low-reasoning default).
- Source: https://openrouter.ai/x-ai/grok-4.5 (2026-07-09).

## 6. Third-party benchmarks (Artificial Analysis via OpenRouter)
GPQA Diamond **93.1** · HLE 40.3 · AA-LCR (long-context reasoning) 67.7 · GDPval-AA 52.1 ·
CritPt 15.4 · SciCode **54.1** · AA-Omniscience Acc 52.0 / Non-Hallucination 46.5.
Honest read: flagship-tier reasoning + fast + cheap cached input, but GPQA 93.1 is **below**
the planner incumbent gemini-3.1-pro (94.3), and SciCode 54.1 is not evidence of an executor
upgrade vs Opus (SWE-bench 88.6). => grok-4.5 is a strong cross-family CANDIDATE, not a proven
role-beater. No "beats Opus/GPT-5.5" claim ships.

## 7. What shipped in v1.10.0 (Tier 1) and what did NOT
- Shipped: critic slot grok-4.3 → grok-4.5, like-for-like effort (daily :medium; ultimate /
  ultimate-f5 / legend / escalation + cyber-cop high-risk xai panel :high). No `:xhigh`/`:max`.
- NOT shipped: any executor/planner/architect/default promotion of grok-4.5. Main-role
  placement is deferred to a Tier-2 L3 rolefit (modeled on
  `evidence/2026-07-03-l3-fable-rolefit.{md,jsonl}`) evaluating planner / executor / architect
  + critic high-vs-medium A/B, with a separate follow-up PR only if grok-4.5 wins a position.
