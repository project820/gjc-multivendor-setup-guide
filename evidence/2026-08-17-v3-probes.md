# 2026-08-17 — v3 필수 dated 프로브

승인된 ralplan 계획(final stage-03, sha256 `3776aa88…`)이 v3 출하 전 요구한 프로브다.
그 계획 파일은 세션 로컬 산출물이라 이 저장소에 들어오지 않는다 — 여기 기록된 수치와
판정만으로 자립적으로 읽히도록 썼다.
이 파일은 append-only 다. 기존 evidence 파일은 수정하지 않았다.

명령 형태: `gjc -p --no-session --no-tools --model <selector> "<prompt>"`

## 1. 필수 프로브 2건 — 해석(resolve) 확인

| selector | 결과 | 비고 |
|---|---|---|
| `openai-codex/gpt-5.6-luna:max` | **ok** | 해석은 되나 아래 §3 판정 참조 |
| `openai-codex/gpt-daybreak-blue-latest:high` | **ok** | 모델 계열 자기보고 = "GPT" |

### Daybreak 계정 범위 고지

이 프로브는 **Daybreak 승인을 이미 받은 단일 계정**에서 실행됐다. 계정 식별자·조직·승인
경로는 기록하지 않는다(비식별). **일반 가용성 증거가 아니다** — 미승인 사용자는 이 셀렉터가
해석되지 않는다. 따라서 문서에서는 cyber-cop 의 **선택적 수동 교차확인 핀**으로만 다룬다.
미보유자는 기존 구성 그대로 동작하고, Daybreak 실패는 Core 실패도 CI 실패도 아니다.

> **2026-08-17 정정**: 초기 초안은 이것을 "패널 4표째" 로 적었다. `scripts/cyber-cop-review.sh`
> 실측 결과 패널 좌석은 `xai/grok-4.6:high`·`google-antigravity/gemini-3.1-pro-low:high`
> **하드코딩 2개**이고 스크립트 안에 `daybreak` 문자열이 **0건**이다 — 투표로 배선돼 있지 않다.
> 자세한 근거와 삽입 문구는 `.gjc/v3-pending-docs/cyber-cop-daybreak-footnote.md`.

## 2. budget 게이트 3종

| selector | 결과 |
|---|---|
| `opencode-go/qwen3.8-max` | **ok** |
| `opencode-go/glm-5.2` | **ok** |
| `opencode-go/minimax-m3` | **ok** |

3종 모두 해석된다. 다만 계획이 못박은 대로 **planner 좌석은 qwen3.8-max 단일 확정**이며
minimax 는 채택하지 않은 재평가 후보로만 남는다(대체 실행 경로 없음).

## 3. Luna `:max` vs `:xhigh` — 사전등록 비교 (핵심)

### 1순위 판별기: 없음

세션 JSONL 에 `thinkingLevel` 필드가 있어 처음엔 판별기로 보였다. 그러나 대조 실험 결과
**요청값을 그대로 되비칠 뿐 프로바이더가 실제 적용한 값이 아니다**:

| 요청 | 기록된 thinkingLevel |
|---|---|
| `gpt-5.6-luna:medium` | medium |
| `gpt-5.6-luna:xhigh` | xhigh |
| `gpt-5.6-luna:max` | max |
| `gpt-5.6-sol:max` | max ← sol 출하 상한은 xhigh 인데도 max 로 기록 |

`sol:max` 까지 `max` 로 찍히므로 이 필드는 클램프 상류에 있다. **관찰 가능한 provider-resolved
effort 메타데이터는 없다.** 따라서 2순위 통계 절차로 갔다.

### 2순위: 사전등록 paired 설계

- 태스크 10개(산술·조합론·정수론), 정답 자동 채점. 채점 기준은 실행 전 고정, 사후 조정 없음.
- 각 태스크 × {`:max`, `:xhigh`} × 3반복 = **60콜**
- 지표: 정답(0/1), output 토큰 수, 지연
- 합격 규칙(사전 고정): Wilcoxon signed-rank p<0.05 단측 **AND** 최소 4/10 태스크에서
  max 평균이 xhigh 대비 +0.1 이상 **AND** 그 우위가 각 태스크 3회 중 2회 이상 재현

| task | max 정답 | xhigh 정답 | 점수차 | max 출력토큰 | xhigh 출력토큰 | 토큰차 |
|---|---|---|---|---|---|---|
| t01 | 1.00 | 1.00 | +0.00 | 29.3 | 22.7 | +6.7 |
| t02 | 1.00 | 1.00 | +0.00 | 46.0 | 42.3 | +3.7 |
| t03 | 1.00 | 0.67 | +0.33 | 54.3 | 45.0 | +9.3 |
| t04 | 1.00 | 1.00 | +0.00 | 46.3 | 17.3 | +29.0 |
| t05 | 1.00 | 1.00 | +0.00 | 41.3 | 26.0 | +15.3 |
| t06 | 1.00 | 1.00 | +0.00 | 30.3 | 11.7 | +18.7 |
| t07 | 1.00 | 1.00 | +0.00 | 68.0 | 61.3 | +6.7 |
| t08 | 1.00 | 1.00 | +0.00 | 65.7 | 40.3 | +25.3 |
| t09 | 1.00 | 1.00 | +0.00 | 43.0 | 37.7 | +5.3 |
| t10 | 1.00 | 1.00 | +0.00 | 46.3 | 17.3 | +29.0 |

### 결과

- 정확도: **max 30/30 · xhigh 29/30**. 점수차가 0 이 아닌 태스크는 t03 하나뿐(+0.33).
- 최소 효과크기 규칙: **0/10 태스크** 충족(요구 4/10) — t03 도 3회 중 1회만 우위라 재현 조건 미달.
- Wilcoxon signed-rank (단측, `scipy.stats.wilcoxon(alternative='greater')`):
  **stat=1.0, p=0.1587** → α=0.05 미달, **유의성 없음**. 30쌍 중 **29쌍이 정확히 동점**이고
  비영 차이는 t03 의 1쌍뿐이라 검정력이 사실상 없다 — 이것이 "천장 효과" 의 실체다.
- 출력 토큰: **max 평균 47.1 · xhigh 평균 32.2** (n=30 each). 10개 태스크 전부에서 max 가 더 김.

### 판정: **FAIL** (사전등록 규칙 기준)

해석을 정확히 적는다:

1. **침묵 클램프 증거는 아니다.** 출력 토큰이 10/10 태스크에서 체계적으로 다르다
   (max 가 항상 더 김, 평균 +14.9). 클램프라면 두 조건이 구분되지 않아야 한다.
   `:max` 는 실제로 다르게 동작한다.
2. **품질 이점 증거도 아니다.** 이 태스크셋에서 두 조건 모두 사실상 만점이라
   **천장 효과**로 변별이 안 된다. 더 어려운 코퍼스가 필요하다.
3. 즉 이 배터리가 말할 수 있는 것은 "**이 난이도에서는 `:max` 가 토큰만 더 쓰고
   정확도 이득이 없다**" 이다.

### 계획이 정한 처리 (준수)

승인된 계획과 deep-interview R8 잠금은 FAIL 시 **자동 `:xhigh` 대체를 금지**한다.
따라서 `daily.executor = openai-codex/gpt-5.6-luna:max` 좌석은 근거 미달이며,
**v3 릴리스를 차단하고 사용자 재결정으로 되돌린다.**

원시 60콜 보존 위치(**출하 대상은 앞쪽**):
- `.gjc/v3-pending-evidence/luna_raw.json` — v3 브랜치에서 `evidence/` 로 이동한다.
  **이동 시 이름을 바꿔야 한다**: 현행 `evidence/` 32개 파일이 **예외 없이 `YYYY-MM-DD-` 접두**를
  쓴다(2026-08-17 확인). `luna_raw.json` 그대로 넣으면 유일한 무날짜 파일이 된다.
  권장 이름 `evidence/2026-08-17-luna-max-raw.json`. 구조화 데이터 선례는
  `2026-07-03-l3-fable-rolefit.jsonl` 이 있다(이 파일은 JSON 배열이라 `.json` 이 정직하다).
- `.gjc/qa-artifacts/luna_raw.json` — 런 작업 사본. 두 파일은 sha256 동일
  (`86c13f46611f53ae…`) 임을 확인했다. 이동 후 이 사본은 정본이 아니다.

**계획과의 파일명 편차**: 승인 계획은 원시 보존처를
`evidence/<날짜>-v3-luna-max-comparison.md` 로 지정했다. 실제로는 이 문서
(`2026-08-17-v3-probes.md`)의 부록에 전수표를 싣고 `luna_raw.json` 을 함께 뒀다 —
Daybreak·Qwen 프로브와 한 문서로 묶는 편이 읽기 좋아서다. **내용 요건은 충족하지만
파일명은 계획과 다르다.** v3 착수 시 계획대로 분리할지 이대로 둘지 정할 것.

### 사후 재현 검증 (2026-08-17, `luna_raw.json` 에서 독립 재계산)

위 집계·판정을 원시 JSON 에서 다시 계산해 대조했다. **불일치 0건**:

| 항목 | 문서 기재 | 재계산 |
|---|---|---|
| 총 콜 / 설계 | 60 | 60 (10 태스크 × 2 effort × 3 rep) |
| 정확도 | max 30/30 · xhigh 29/30 | 동일 |
| 비영 점수차 태스크 | t03 뿐 (+0.33) | 동일 (+0.333) |
| 효과크기 충족 | 0/10 | 0/10 — t03 은 차이 +0.333 이나 재현 1/3 로 조건 미달 |
| 열등 태스크 | 없음 | 0 |
| 출력 토큰 평균 | 47.1 / 32.2 | 동일 |
| max 가 더 긴 태스크 | 10/10 | 10/10 |
| `thinkingLevel` | 요청값을 그대로 반영 | `max→max`, `xhigh→xhigh` (1순위 판별자 부재 확인) |

### 사용자 결정이 필요한 선택지

1. `daily.executor` 를 `:xhigh` 로 내린다 — 이 배터리 기준 품질 동일, 토큰 절감.
   단 deep-interview R8 의 잠금 결정을 바꾸는 것이므로 사용자 승인이 필요하다.
2. 더 어려운 코퍼스로 재측정한다(천장 효과 제거) — 비용 추가, 결과 미지.
3. 근거 부족을 감수하고 `:max` 를 그대로 출하한다 — 이 레포의
   "검증 없는 셀렉터 출하 금지" 원칙과 정면 충돌하므로 권장하지 않는다.

### 2026-08-17 — 사용자 결정 (append, 위 판정은 그대로 둔다)

사용자가 선택지 **3번(그대로 `:max` 출하)** 을 골랐다. 원문: **"luna 는 max 만 허용한다"**.

**위 §3 의 FAIL 판정은 수정하지 않는다.** 사전등록 규칙 기준으로 배터리는 FAIL 이
맞고, 그 사실이 이 문서의 기록 가치다. 좌석이 `:max` 로 간 것은 **측정이 이겨서가
아니라 사용자 정책 결정**이며, 둘은 다른 층위다.

결정이 추가로 좁힌 것 — 사용자 문면의 "**만**" 을 규칙으로 받았다:

| 항목 | 계획 원안 | 결정 반영 |
|---|---|---|
| `daily.executor` | `gpt-5.6-luna:max` (배터리 통과 조건부) | **`gpt-5.6-luna:max` 확정** |
| D-1 합법 effort | `{low, medium, high, xhigh, max}` | **`{max}` 단독** |

실측(임시 트리): validator `exit 0` · `luna:xhigh` → `exit 1 illegal effort 'xhigh'
for gpt-5.6-luna (legal: ['max'])` · `luna:medium` 도 동일하게 거부.
v2.1.0 에서 합법이던 `luna:medium`(`eco.planner`)이 이제 거부되는 것이 규칙이 실제로
좁아졌다는 증거다.

> **"검증 없는 셀렉터 출하 금지" 와의 관계** — 선택지 3번은 그 원칙과 충돌한다고
> 위에 적었는데, 정확히는 **충돌하지 않는다**. 그 원칙이 요구하는 것은 **dated
> 실호출**이고 `luna:max` 는 §1 에서 `ok` 로 해석됐다. 이 배터리가 본 것은
> *해석 가능성*이 아니라 *품질 이득*이다. 다만 **문서가 "검증된 우위" 로 읽히면
> 안 된다** — 출하 문서 문구안은 `MAINTAINING-v3-updates.md` §6 에 있다.

---

## 부록 — 원시 60콜 전수 기록

집계표만으로는 재현이 불가능하므로 개별 호출을 전부 남긴다.

| # | task | effort | rep | 정답 | 출력토큰 | 지연(s) | thinkingLevel |
|---|---|---|---|---|---|---|---|
| 1 | t01 | max | 0 | 1 | 30 | 3.4 | max |
| 2 | t01 | max | 1 | 1 | 29 | 3.0 | max |
| 3 | t01 | max | 2 | 1 | 29 | 3.1 | max |
| 4 | t01 | xhigh | 0 | 1 | 23 | 3.3 | xhigh |
| 5 | t01 | xhigh | 1 | 1 | 22 | 3.1 | xhigh |
| 6 | t01 | xhigh | 2 | 1 | 23 | 5.1 | xhigh |
| 7 | t02 | max | 0 | 1 | 42 | 3.3 | max |
| 8 | t02 | max | 1 | 1 | 49 | 3.7 | max |
| 9 | t02 | max | 2 | 1 | 47 | 3.4 | max |
| 10 | t02 | xhigh | 0 | 1 | 36 | 3.4 | xhigh |
| 11 | t02 | xhigh | 1 | 1 | 49 | 3.3 | xhigh |
| 12 | t02 | xhigh | 2 | 1 | 42 | 3.0 | xhigh |
| 13 | t03 | max | 0 | 1 | 56 | 3.8 | max |
| 14 | t03 | max | 1 | 1 | 51 | 3.8 | max |
| 15 | t03 | max | 2 | 1 | 56 | 3.9 | max |
| 16 | t03 | xhigh | 0 | 1 | 47 | 5.2 | xhigh |
| 17 | t03 | xhigh | 1 | 1 | 43 | 3.7 | xhigh |
| 18 | t03 | xhigh | 2 | 0 | 45 | 3.3 | xhigh |
| 19 | t04 | max | 0 | 1 | 37 | 3.2 | max |
| 20 | t04 | max | 1 | 1 | 75 | 3.7 | max |
| 21 | t04 | max | 2 | 1 | 27 | 2.6 | max |
| 22 | t04 | xhigh | 0 | 1 | 5 | 2.3 | xhigh |
| 23 | t04 | xhigh | 1 | 1 | 23 | 3.8 | xhigh |
| 24 | t04 | xhigh | 2 | 1 | 24 | 3.1 | xhigh |
| 25 | t05 | max | 0 | 1 | 41 | 3.5 | max |
| 26 | t05 | max | 1 | 1 | 42 | 4.2 | max |
| 27 | t05 | max | 2 | 1 | 41 | 3.7 | max |
| 28 | t05 | xhigh | 0 | 1 | 36 | 3.2 | xhigh |
| 29 | t05 | xhigh | 1 | 1 | 5 | 3.5 | xhigh |
| 30 | t05 | xhigh | 2 | 1 | 37 | 3.0 | xhigh |
| 31 | t06 | max | 0 | 1 | 31 | 3.1 | max |
| 32 | t06 | max | 1 | 1 | 30 | 4.8 | max |
| 33 | t06 | max | 2 | 1 | 30 | 3.6 | max |
| 34 | t06 | xhigh | 0 | 1 | 25 | 3.0 | xhigh |
| 35 | t06 | xhigh | 1 | 1 | 5 | 2.5 | xhigh |
| 36 | t06 | xhigh | 2 | 1 | 5 | 2.3 | xhigh |
| 37 | t07 | max | 0 | 1 | 70 | 4.3 | max |
| 38 | t07 | max | 1 | 1 | 69 | 3.8 | max |
| 39 | t07 | max | 2 | 1 | 65 | 3.6 | max |
| 40 | t07 | xhigh | 0 | 1 | 64 | 3.5 | xhigh |
| 41 | t07 | xhigh | 1 | 1 | 60 | 3.6 | xhigh |
| 42 | t07 | xhigh | 2 | 1 | 60 | 3.5 | xhigh |
| 43 | t08 | max | 0 | 1 | 58 | 3.4 | max |
| 44 | t08 | max | 1 | 1 | 72 | 4.0 | max |
| 45 | t08 | max | 2 | 1 | 67 | 3.7 | max |
| 46 | t08 | xhigh | 0 | 1 | 41 | 3.5 | xhigh |
| 47 | t08 | xhigh | 1 | 1 | 39 | 3.9 | xhigh |
| 48 | t08 | xhigh | 2 | 1 | 41 | 3.1 | xhigh |
| 49 | t09 | max | 0 | 1 | 43 | 3.2 | max |
| 50 | t09 | max | 1 | 1 | 42 | 4.4 | max |
| 51 | t09 | max | 2 | 1 | 44 | 3.7 | max |
| 52 | t09 | xhigh | 0 | 1 | 39 | 3.2 | xhigh |
| 53 | t09 | xhigh | 1 | 1 | 38 | 4.5 | xhigh |
| 54 | t09 | xhigh | 2 | 1 | 36 | 3.6 | xhigh |
| 55 | t10 | max | 0 | 1 | 44 | 3.1 | max |
| 56 | t10 | max | 1 | 1 | 47 | 6.1 | max |
| 57 | t10 | max | 2 | 1 | 48 | 3.2 | max |
| 58 | t10 | xhigh | 0 | 1 | 5 | 2.6 | xhigh |
| 59 | t10 | xhigh | 1 | 1 | 42 | 3.1 | xhigh |
| 60 | t10 | xhigh | 2 | 1 | 5 | 2.3 | xhigh |

기계 판독용 원본: `.gjc/v3-pending-evidence/luna_raw.json` (60행, sha256 `86c13f46611f53ae…`).
`.gjc/qa-artifacts/luna_raw.json` 은 동일 내용의 런 작업 사본이다.
채점 스크립트는 `/tmp/luna_probe.py` 로 실행했고 태스크·정답 판정식은 실행 전 고정했다.

**그 하버스를 `2026-08-17-luna-probe-harness.py` 로 보존했다**(이 디렉터리, `/tmp` 원본과 sha256 동일).
`/tmp` 는 재부팅에 날아가므로, 사전등록 채점기를 잃으면 결정 선택지 (b)"더 어려운 코퍼스로
재측정" 을 할 때 **같은 채점기로 비교할 수 없다** — 재구성한 채점기는 사전등록 채점기가 아니다.
태스크 10종(산술·조합론·정수론)과 정답 정규식, `REPS=3`, `EFFORTS=["max","xhigh"]`,
`MODEL="openai-codex/gpt-5.6-luna"` 가 그 안에 그대로 있다.
