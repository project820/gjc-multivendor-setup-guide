# Errata — 2026-07-10

Published `evidence/` reports are an append-only audit trail (MAINTAINING §2: dated, never
rewritten). Corrections ship as new dated errata entries here; the original files stay verbatim.

## E1 · `2026-07-10-two-track-sol-notes.md` §1 benchmark table — BrowseComp row column misattribution

- **What's wrong:** the BrowseComp row places `84.3†` in the **Fable 5** column and `85.9†` in the
  **Opus 4.8** column.
- **Correction:** the official OpenAI GPT-5.6 table (openai.com/index/gpt-5-6, re-rendered
  2026-07-10) has **no Fable 5 value** in that row. `84.3` belongs to **Opus 4.8** and `85.9`
  belongs to **Gemini 3.1 Pro Preview** — the values were shifted one column left when transcribed.
  Sol 90.4 and the "Sol browsing 우위" reading are unchanged.
- **Impact:** none on the two-track verdict (browsing was already a Sol axis). Downstream copies in
  `research/2026-07-10-gjc-v2-profile-review/research-pack-v0.md` §4 inherited the same shift and
  must be read with this erratum. Any future guide copy citing Fable/Opus BrowseComp values must
  use the corrected attribution.
- **Found by:** both independent v2-review research lanes (Claude Fable 5 Ultracode raw report §4-1;
  citation audit re-verification) against the primary OpenAI table.

## E2 · Luna Terminal-Bench 2.1 — unresolved 84.3 vs 84.7 between same-day notes

- **What's wrong:** `2026-07-10-gpt-5.6-notes.md` §1 cites Luna Terminal-Bench 2.1 as **84.3%**,
  while `2026-07-10-two-track-sol-notes.md` §1 (later direct render of the same official table)
  records **84.7**. The discrepancy shipped without an erratum.
- **Correction:** adopt **84.7** (the later direct-render value) as the working figure; treat 84.3
  as a transcription-era value superseded by the re-render. Neither value has independent
  (non-OpenAI) confirmation; both remain vendor-table figures.
- **Impact:** none on verdicts — Luna holds no seat in the current candidate portfolio and the
  ±0.4pt spread does not cross any decision threshold. Recorded to keep the audit trail honest.
- **Found by:** Claude Fable 5 Ultracode research lane (P4-AUX), confirmed by citation audit.

## R1 · grok-build catalog re-check (2026-07-10 14:4x KST live re-query) — not an erratum

- `gjc --list-models grok-build` still lists only `grok-4.20-*`, `grok-4.3`, `grok-build`,
  `grok-composer-2.5-fast`. **No `grok-4.5` under `grok-build`.** `grok-4.5` resolves only as
  `xai/grok-4.5` (GJC connector surface: 222K ctx / 8.9K max-out).
- Web-side reports that Grok Build's default model is 4.5 do **not** change the GJC surface today:
  every profile seating `grok-4.5` still requires the **xAI API key** path, and the
  "subscription-only activation" counts in the v2 research remain valid as of this re-check.
- Re-evaluation trigger stands: if `grok-4.5` appears under `grok-build` in a future catalog
  snapshot, recompute the auth/cost claims for all xai-seated profiles.
